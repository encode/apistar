language: python

cache: pip

python:
    - "pypy3.5"
    - "3.5"
    - "3.6"

install:
    - pip install -U pip poetry
    - poetry install -E async -E jinja -E static

script:
    - scripts/lint
    - scripts/test

after_script:
    - codecov
