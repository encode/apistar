sudo: required
dist: xenial
language: python

cache: pip

python:
    - "3.6"
    - "3.7"

install:
    - pip install -U -r requirements.txt

script:
    - scripts/lint
    - scripts/test

after_script:
    - codecov
