sudo: required
dist: xenial
language: python

cache: pip

python:
    - "3.6"
    - "3.7"
    - "3.8"
    - "pypy3"

install:
    - pip install -U -r requirements.txt

script:
    - scripts/ci

after_script:
    - codecov
