#!/bin/sh

set -e

export PREFIX=""
if [[ -d 'venv' ]] ; then
    export PATH="venv/bin/"
    export PREFIX=$PATH
fi


echo "PYTHONPATH=. ${PREFIX}py.test --ignore venv --ignore apistar/project_templates $@"
PYTHONPATH=. py.test --ignore venv --ignore apistar/project_templates $@
