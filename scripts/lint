#!/usr/bin/env python
from subprocess import run

import click


@click.command()
def lint():
    commands = [
        ['flake8', '.'],
        ['isort', '--check-only']
    ]

    for command in commands:
        run(command, check=True)


if __name__ == '__main__':
    lint()
