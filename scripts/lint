#!/bin/sh

set -e

export PREFIX=""
if [[ -d 'venv' ]] ; then
    export PATH="venv/bin/"
    export PREFIX=$PATH
fi


echo "${PREFIX}flake8 apistar tests"
flake8 apistar tests
echo "${PREFIX}isort --check-only --recursive apistar tests"
isort --check-only --recursive apistar tests
